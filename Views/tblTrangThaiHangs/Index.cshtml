@model IEnumerable<WebBanHang.Models.Model.tblTrangThaiHang>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.iIDHang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sThanhToan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idMaGiam)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.iIDHang)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sThanhToan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idMaGiam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.iIDNguoiDung">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.iIDNguoiDung">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.iIDNguoiDung">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
