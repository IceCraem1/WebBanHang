@model IEnumerable<WebBanHang.Models.Model.tblLoaiHang>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.sHang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sDang)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.sHang)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sDang)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.iIDLoai">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.iIDLoai">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.iIDLoai">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
